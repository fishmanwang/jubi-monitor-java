<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jubi.dao.UserExtDao">

    <resultMap id="userMap" type="com.jubi.dao.vo.UserAdminVo">
        <id column="id" jdbcType="INTEGER" property="id"/>
    </resultMap>

    <select id="queryUser" parameterType="com.jubi.dao.param.UserQueryParam" resultMap="userMap">
        SELECT
        u.id id,
        u.username,
        a.nickname nickname,
        a.phone phone,
        a.email email,
        u.frozen frozen,
        u.last_login_ip lastLoginIp,
        u.last_login_time lastLoginTime
        FROM zx_user u LEFT JOIN zx_account a ON u.id = a.user_id
        where 1 = 1
        <if test="username != null">
            and u.username = #{username}
        </if>
        <if test="nickname != null">
            and u.nickname = #{nickname}
        </if>
        order by last_login_time desc
    </select>

</mapper>
